import { EffetCarDirective } from './effet-car.directive';

describe('EffetCarDirective', () => {
  it('should create an instance', () => {
    const directive = new EffetCarDirective();
    expect(directive).toBeTruthy();
  });
});
